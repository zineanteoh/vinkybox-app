import 'package:flutter/material.dart';

class DeliverForOthersCard extends StatelessWidget {
  const DeliverForOthersCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
